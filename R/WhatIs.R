
check_it = CheckIt = function(x, ...){
message("\n")
    message(str(x, ...))
message("\n")
return(    invisible(x))
}


what_is = WhatIs = function(x, ...){
message("\n")
    message(VI(x, ...))
message("\n")
return(    invisible(x))
}
